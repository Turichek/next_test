import { Typography } from '@mui/material';
import { FC } from 'react';

const Footer: FC = () => {
    return (
        <footer>
            <Typography variant='h6'>Created by Turik</Typography>
        </footer>
    )
}

export default Footer;